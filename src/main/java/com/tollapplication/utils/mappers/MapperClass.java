package com.tollapplication.utils.mappers;

import java.time.LocalDate;
import java.time.LocalTime;
import java.util.LinkedHashMap;

/**
 * Unused, kept for improvement, using mappers, replacing the default Maps from yaml
 */
public class MapperClass {
    /*public LocalDate mapToTollTimeFee(String holiday) {
        return LocalDate.parse(holiday);
    };

    public TimeFee mapToTollTimeFee(LinkedHashMap<String,String> timeFeeString) {
        return new TimeFee()
                .setStart(LocalTime.parse(timeFeeString.get("start")))
                .setEnd(LocalTime.parse(timeFeeString.get("end")))
                .setFee(Double.valueOf(timeFeeString.get("fee")));
    }*/
}
